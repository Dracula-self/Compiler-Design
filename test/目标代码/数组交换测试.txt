program arrayTest{测试数组交换和赋值}
var integer i, j, temp;
    array[1..10] of integer a;

begin
   i := 2;
   j := 3;
   a[i] := 5;
   a[j] := 3;
   
   write(a[i]); 
   write(a[j]);
   
   temp := a[i];
   a[i] := a[j];
   a[j] := temp;
   
   write(a[i]);
   write(a[j])
end.