program constOptTest{常量表达式优化测试}
{正确结果 15，50,75,6,7,8,10}
var integer a,b,result,i;
begin
  a:=5;
  b:=10;
  
  result:=a+b;
  write(result);
  
  result:=a*b;
  write(result);
  
  result:=(a+b)*(b-a);
  write(result);
  
  i:=0;
  while i<3 do
    i:=i+1;
    result:=a+i;
    write(result)
  endwh;
  
  if a<b
  then result:=a*2
  else result:=b*2
  fi;
    
  write(result)
end.